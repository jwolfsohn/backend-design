{
  "name": "backend-design",
  "version": "0.5.0",
  "description": "Claude Code skill that analyzes any modern web frontend (React, Vue, Svelte, Angular, Astro, Solid, Qwik, Remix, HTMX, vanilla, more) and scaffolds a matching backend in the stack of your choice.",
  "type": "module",
  "bin": {
    "backend-design": "bin/backend-design.mjs"
  },
  "files": [
    "SKILL.md",
    "validate.mjs",
    "bin/",
    "prompts/",
    "scripts/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test:validate": "node validate.mjs",
    "check:bin": "node -e \"const p=require('./package.json');const bad=Object.entries(p.bin||{}).filter(([,v])=>v.startsWith('./'));if(bad.length){console.error('bin paths must not start with ./ — npm strips them silently:',bad);process.exit(1);}console.log('bin paths OK')\"",
    "prepublishOnly": "npm pkg fix && npm run check:bin"
  },
  "dependencies": {
    "picocolors": "^1.1.1",
    "prompts": "^2.4.2"
  },
  "keywords": [
    "claude-code",
    "claude",
    "skill",
    "backend",
    "scaffold",
    "react",
    "nextjs",
    "express",
    "fastify",
    "hono",
    "prisma",
    "drizzle",
    "fastapi",
    "postgres"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jwolfsohn/backend-design.git"
  },
  "bugs": {
    "url": "https://github.com/jwolfsohn/backend-design/issues"
  },
  "homepage": "https://github.com/jwolfsohn/backend-design#readme"
}
